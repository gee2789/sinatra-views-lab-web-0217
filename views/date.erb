<!DOCTYPE html>
<html>
    <head>
        <title>Date Page</title>
    </head>
    <body>
        <% Today = Date.today.strftime("%A, %B %d, %Y") %>
        <h1><%= "The date is #{Today}"%></h1>
    </body>
</html>
